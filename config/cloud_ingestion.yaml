server:
  host: 0.0.0.0
  port: 8100
  workers: 1
  log_level: INFO

database:
  url: sqlite:///tmp/fallback.db   # overridden by GLI_DATABASE_URL env var

auth:
  enabled: true
  api_key: ""

cors:
  allowed_origins:
    - "*"
