# GLI-FLOW Layout Image Pipeline Audit

## Verification
- **Generation**: Images (WEBP) generated by OpenRoad during the `SIGN_OFF` stage.
- **Storage**: Stored in `run_dir/reports/`.
- **Indexing**: API dynamically scans for `*.webp` in `reports/`.
- **Exposure**: Served via `/runs/{run_id}/image/{name}`.
- **Rendering**: Correctly displayed in `RunDetail.jsx` (LayoutImagesTab).

**Audit Verdict:** Layout image pipeline fully operational.
